import { FeedbackRatingPipe } from './feedback-rating.pipe';

describe('FeedbackRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new FeedbackRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
